//DEFINITIONS
const form = document.getElementById("form_tasks_list");
const tasks_list_ul = document.getElementById("tasks_list");
const button = document.getElementById("taskListButton");

button.addEventListener("click", addTask);

function addTask()
{
    
}
